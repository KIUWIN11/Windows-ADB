name: 🖥️ RDP • Windows 11 • PUBLIC • SIMPLE

on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
    # ===============================
    # ENABLE RDP + FIREWALL
    # ===============================
    - name: 🔐 Enable RDP
      run: |
        Set-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server' fDenyTSConnections 0
        Set-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' UserAuthentication 0
        netsh advfirewall firewall add rule name="RDP-3389" dir=in action=allow protocol=TCP localport=3389
        Restart-Service TermService -Force

    # ===============================
    # CREATE USER (USERNAME GIỮ NGUYÊN)
    # ===============================
    - name: 👤 Create RDP User
      run: |
        $username = "WIN"
        $password = "Rdp@13579!Zx"
        $secure = ConvertTo-SecureString $password -AsPlainText -Force

        if (Get-LocalUser -Name $username -ErrorAction SilentlyContinue) {
          Set-LocalUser -Name $username -Password $secure
        } else {
          New-LocalUser -Name $username -Password $secure -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member $username
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member $username
        }

    # ===============================
    # SHOW IP + KEEP ALIVE
    # ===============================
    - name: 🟢 SHOW LOGIN INFO + KEEP ALIVE
      run: |
        $ip = (Invoke-RestMethod -Uri "https://api.ipify.org")
        Write-Host "================================"
        Write-Host "PUBLIC IP : $ip"
        Write-Host "PORT      : 3389"
        Write-Host "USERNAME  : WIN"
        Write-Host "PASSWORD  : Rdp@13579!Zx"
        Write-Host "================================"

        while ($true) { Start-Sleep 300 }
